let age = Number(prompt('enter the age'));

document.write(`возраст пользователя ${age} БикЮ                                            `)

if (age === 18) {
    document.write(` пользователю ${age}`)
} else {
    document.write(` пользователю не 18`)

}2